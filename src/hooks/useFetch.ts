import { useEffect, useState } from 'react'

const useCalls = () => {
	return {
		fetchItem: () => {},
		fetchItems: () => {},
		createItem: () => {},
		createItems: () => {},
		updateItem: () => {},
		updateItems: () => {},
		deleteItem: () => {},
		deleteItems: () => {},
	}
}
